export default {
    props: {
        zIndex: {
            type: Number,
            default: 100
        },
        // 遮罩是否可关闭
        maskClosable: {
            type: Boolean,
            default: false
        }
    }
}